{
  "name": "db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "migrate": "supabase db push",
    "reset": "supabase db reset",
    "types": "supabase gen types typescript --linked --schema public 2>/dev/null | grep -v '^Initialising\\|^A new version\\|^We recommend' > ../../apps/web/src/lib/supabase/database.types.ts",
    "seed": "tsx src/seed.ts",
    "test": "echo 'no db tests yet' && exit 0"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.49.4",
    "supabase": "2.22.6",
    "tsx": "^4.19.0"
  }
}
