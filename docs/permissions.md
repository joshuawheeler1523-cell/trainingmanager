# Permissions Reference (generated)

_Generated by `pnpm tsx scripts/generate-permissions-doc.ts`. Do not edit by hand._

This file is the canonical record of role gating across the app. It is built from two sources:

1. `@requiredRole` JSDoc tags on every server action (under `apps/web/src/app/**/actions.ts`).
2. `CREATE POLICY` statements in `supabase/migrations/*.sql`.

**Note on the policies section**: this is parsed from migration files. Policies dropped via dynamic SQL (e.g. the Phase 3/4 DO blocks that introspect `pg_policies`) won't be subtracted here — they may appear as legacy entries even though the live DB has replaced them. For the authoritative current state, query `pg_policies` directly. The Phase 3 and Phase 4 migrations document the canonical post-rewrite policy names (`<table>_write`, `<table>_manager_all`, `<table>_instructor_*`, `<table>_self_*`).

If anything in the actions section looks wrong, fix the source and re-run.

## Server actions

Total annotated actions: **10**

| File                                                                   | Action                       | Required role |
| ---------------------------------------------------------------------- | ---------------------------- | ------------- |
| `apps/web/src/app/(authenticated)/admin/actions.ts`                    | `inviteUser`                 | `manager`     |
| `apps/web/src/app/(authenticated)/admin/actions.ts`                    | `removeMember`               | `manager`     |
| `apps/web/src/app/(authenticated)/admin/actions.ts`                    | `resendInvitation`           | `manager`     |
| `apps/web/src/app/(authenticated)/admin/actions.ts`                    | `revokeInvitation`           | `manager`     |
| `apps/web/src/app/(authenticated)/admin/actions.ts`                    | `setFeatureFlag`             | `manager`     |
| `apps/web/src/app/(authenticated)/admin/actions.ts`                    | `updateMember`               | `manager`     |
| `apps/web/src/app/(authenticated)/admin/actions.ts`                    | `updateOrgSettings`          | `manager`     |
| `apps/web/src/app/(authenticated)/admin/settings/workspace/actions.ts` | `applyWorkspacePresetAction` | `manager`     |
| `apps/web/src/app/(authenticated)/admin/settings/workspace/actions.ts` | `setModuleFlagAction`        | `manager`     |
| `apps/web/src/app/(authenticated)/admin/settings/workspace/actions.ts` | `updateLabelOverridesAction` | `manager`     |

## RLS policies (current)

Total active policies (latest definition wins): **134**

| Table                           | Policy                                           | Cmd    | Predicate (truncated)                                                                                                                                                                                      |
| ------------------------------- | ------------------------------------------------ | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ad_hoc_tasks`                  | `ad_hoc_tasks_modify`                            | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `ad_hoc_tasks`                  | `ad_hoc_tasks_select`                            | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `allocation_buckets`            | `alloc_buckets_modify`                           | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `allocation_buckets`            | `alloc_buckets_select`                           | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `allocation_group_members`      | `alloc_group_members_modify`                     | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `allocation_group_members`      | `alloc_group_members_select`                     | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `allocation_groups`             | `alloc_groups_modify`                            | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `allocation_groups`             | `alloc_groups_select`                            | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `audit_log`                     | `members can view audit log for their org`       | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `audit_logs`                    | `org members can insert audit logs`              | INSERT | `with check (public.is_org_member(org_id))`                                                                                                                                                                |
| `audit_logs`                    | `org members can view audit logs`                | SELECT | `using (public.is_org_member(org_id))`                                                                                                                                                                     |
| `class_instructor_assignments`  | `cia_modify`                                     | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `class_instructor_assignments`  | `cia_select`                                     | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `class_skill_requirements`      | `csr_modify`                                     | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `class_skill_requirements`      | `csr_select`                                     | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `classes`                       | `classes_modify`                                 | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `classes`                       | `classes_select`                                 | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `deliverable_types`             | `deliverable_types_modify`                       | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `deliverable_types`             | `deliverable_types_select`                       | SELECT | `using ( org_id is null or org_id in (select public.user_org_ids()) )`                                                                                                                                     |
| `department_memberships`        | `admins manage department memberships`           | ALL    | `using ( department_id in ( select d.id from public.departments d where is_org_admin(d.org_id) or is_department_admin(d.id) ) ) with check ( department_id in ( select d.id from public.departments d whe` |
| `department_memberships`        | `users see relevant department memberships`      | SELECT | `using ( user_id = auth.uid() or department_id in ( select d.id from public.departments d where is_org_admin(d.org_id) ) )`                                                                                |
| `departments`                   | `members can see departments in their orgs`      | SELECT | `using ( org_id in (select org_id from public.org_memberships where user_id = auth.uid() and accepted_at is not null) )`                                                                                   |
| `departments`                   | `org admins manage departments`                  | ALL    | `using (is_org_admin(org_id)) with check (is_org_admin(org_id))`                                                                                                                                           |
| `dependencies`                  | `dependencies_modify`                            | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `dependencies`                  | `dependencies_public_share_select`               | SELECT | `to anon using ( exists ( select 1 from public.projects p where p.id = dependencies.project_id and p.public_share_token is not null and p.public_share_token = public.get_pg_share_token() ) )`            |
| `dependencies`                  | `dependencies_select`                            | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `education_request_assignments` | `edu_req_assignments_instructor_self`            | ALL    | `USING ( public.is_instructor(org_id) AND instructor_id = public.current_instructor_id(org_id) ) WITH CHECK ( public.is_instructor(org_id) AND instructor_id = public.current_instructor_id(org_id) )`     |
| `education_request_assignments` | `edu_req_assignments_manager_all`                | ALL    | `USING (public.is_manager(org_id)) WITH CHECK (public.is_manager(org_id))`                                                                                                                                 |
| `education_request_assignments` | `education_request_assignments_modify`           | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `education_request_assignments` | `education_request_assignments_select`           | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `education_request_history`     | `education_request_history_select`               | SELECT | `using ( org_id in (select user_org_ids()) and ( is_org_admin(org_id) or department_id in (select user_department_ids()) ) )`                                                                              |
| `education_requests`            | `education_requests_insert_public_anon`          | INSERT | `to anon with check ( submitted_via = 'public_form' and status = 'new' and public_form_token is not null and exists ( select 1 from public.public_intake_links pil where pil.token = education_requests.p` |
| `education_requests`            | `education_requests_modify`                      | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `education_requests`            | `education_requests_select`                      | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `feature_flags`                 | `authenticated users can read global flags`      | SELECT | `using ( org_id is null or org_id in (select public.user_org_ids()) )`                                                                                                                                     |
| `feature_flags`                 | `org admins can manage org flags`                | ALL    | `using (public.is_org_admin(org_id)) with check (public.is_org_admin(org_id))`                                                                                                                             |
| `global_allocations`            | `global_alloc_modify`                            | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `global_allocations`            | `global_alloc_select`                            | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `group_allocations`             | `group_alloc_modify`                             | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `group_allocations`             | `group_alloc_select`                             | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `impl_class_prerequisites`      | `impl_class_prereqs_modify`                      | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `impl_class_prerequisites`      | `impl_class_prereqs_select`                      | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `impl_class_trainers`           | `impl_class_trainers_modify`                     | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `impl_class_trainers`           | `impl_class_trainers_select`                     | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `impl_classes`                  | `impl_classes_modify`                            | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `impl_classes`                  | `impl_classes_select`                            | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `impl_modules`                  | `impl_modules_modify`                            | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `impl_modules`                  | `impl_modules_select`                            | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `impl_rooms`                    | `impl_rooms_modify`                              | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `impl_rooms`                    | `impl_rooms_select`                              | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `impl_sessions`                 | `impl_sessions_modify`                           | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `impl_sessions`                 | `impl_sessions_select`                           | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `impl_trainers`                 | `impl_trainers_modify`                           | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `impl_trainers`                 | `impl_trainers_select`                           | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `implementations`               | `implementations_modify`                         | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `implementations`               | `implementations_select`                         | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `individual_allocations`        | `indiv_alloc_modify`                             | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `individual_allocations`        | `indiv_alloc_select`                             | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `individual_allocations`        | `individual_allocations_manager_all`             | ALL    | `USING (public.is_manager(org_id)) WITH CHECK (public.is_manager(org_id))`                                                                                                                                 |
| `individual_allocations`        | `individual_allocations_self_update`             | UPDATE | `USING ( public.is_instructor(org_id) AND instructor_id = public.current_instructor_id(org_id) ) WITH CHECK ( public.is_instructor(org_id) AND instructor_id = public.current_instructor_id(org_id) )`     |
| `instructor_skills`             | `instructor_skills_manager_all`                  | ALL    | `USING (public.is_manager(org_id)) WITH CHECK (public.is_manager(org_id))`                                                                                                                                 |
| `instructor_skills`             | `instructor_skills_modify`                       | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `instructor_skills`             | `instructor_skills_select`                       | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `instructor_skills`             | `instructor_skills_self_all`                     | ALL    | `USING ( public.is_instructor(org_id) AND instructor_id = public.current_instructor_id(org_id) ) WITH CHECK ( public.is_instructor(org_id) AND instructor_id = public.current_instructor_id(org_id) )`     |
| `instructors`                   | `instructors_manager_all`                        | ALL    | `USING (public.is_manager(org_id)) WITH CHECK (public.is_manager(org_id))`                                                                                                                                 |
| `instructors`                   | `instructors_modify`                             | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `instructors`                   | `instructors_select`                             | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `instructors`                   | `instructors_self_update`                        | UPDATE | `USING (public.is_instructor(org_id) AND user_id = auth.uid()) WITH CHECK (public.is_instructor(org_id) AND user_id = auth.uid())`                                                                         |
| `milestones`                    | `milestones_modify`                              | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `milestones`                    | `milestones_public_share_select`                 | SELECT | `to anon using ( exists ( select 1 from public.projects p where p.id = milestones.project_id and p.public_share_token is not null and p.public_share_token = public.get_pg_share_token() ) )`              |
| `milestones`                    | `milestones_select`                              | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `notifications`                 | `notifications_select_own`                       | SELECT | `using ( recipient_id = auth.uid() or org_id in (select public.user_org_ids()) )`                                                                                                                          |
| `notifications`                 | `notifications_update_own`                       | UPDATE | `using (recipient_id = auth.uid()) with check (recipient_id = auth.uid())`                                                                                                                                 |
| `org_invitations`               | `org admins can manage invitations`              | ALL    | `using (public.is_org_admin(org_id)) with check (public.is_org_admin(org_id))`                                                                                                                             |
| `org_invitations`               | `users can accept their own pending invitations` | UPDATE | `using ( email::text = (select email from auth.users where id = auth.uid()) and accepted_at is null and expires_at > now() ) with check ( email::text = (select email from auth.users where id = auth.uid` |
| `org_invitations`               | `users can view their own pending invitations`   | SELECT | `using ( email::text = (select email from auth.users where id = auth.uid()) and accepted_at is null and expires_at > now() )`                                                                              |
| `org_memberships`               | `members can view memberships in their orgs`     | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `org_memberships`               | `org admins can delete members`                  | DELETE | `using (public.is_org_admin(org_id))`                                                                                                                                                                      |
| `org_memberships`               | `org admins can insert members`                  | INSERT | `with check (public.is_org_admin(org_id))`                                                                                                                                                                 |
| `org_memberships`               | `org admins can update members`                  | UPDATE | `using (public.is_org_admin(org_id))`                                                                                                                                                                      |
| `org_memberships`               | `users can join via valid invitation`            | INSERT | `with check ( user_id = auth.uid() and exists ( select 1 from public.org_invitations inv where inv.org_id = org_memberships.org_id and inv.email::text = (select email from auth.users where id = auth.ui` |
| `organization_members`          | `org admins can add members`                     | INSERT | `with check (public.is_org_admin(org_id))`                                                                                                                                                                 |
| `organization_members`          | `org admins can delete members`                  | DELETE | `using (public.is_org_admin(org_id))`                                                                                                                                                                      |
| `organization_members`          | `org admins can update members`                  | UPDATE | `using (public.is_org_admin(org_id))`                                                                                                                                                                      |
| `organization_members`          | `org members can view members`                   | SELECT | `using (public.is_org_member(org_id))`                                                                                                                                                                     |
| `organizations`                 | `members can view their org`                     | SELECT | `using (id in (select public.user_org_ids()))`                                                                                                                                                             |
| `organizations`                 | `org admins can update their org`                | UPDATE | `using (public.is_org_admin(id))`                                                                                                                                                                          |
| `organizations`                 | `org members can view org`                       | SELECT | `using (public.is_org_member(id))`                                                                                                                                                                         |
| `project_team_members`          | `project_team_members_modify`                    | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `project_team_members`          | `project_team_members_select`                    | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `project_team_members`          | `team_members_public_share_select`               | SELECT | `to anon using ( exists ( select 1 from public.projects p where p.id = project_team_members.project_id and p.public_share_token is not null and p.public_share_token = public.get_pg_share_token() ) )`    |
| `projects`                      | `projects_instructor_team_update`                | UPDATE | `USING ( public.is_instructor(projects.org_id) AND EXISTS ( SELECT 1 FROM public.project_team_members ptm WHERE ptm.project_id = projects.id AND ptm.instructor_id = public.current_instructor_id(project` |
| `projects`                      | `projects_manager_all`                           | ALL    | `USING (public.is_manager(org_id)) WITH CHECK (public.is_manager(org_id))`                                                                                                                                 |
| `projects`                      | `projects_modify`                                | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `projects`                      | `projects_public_share_select`                   | SELECT | `to anon using ( public_share_token is not null and public_share_token = public.get_pg_share_token() )`                                                                                                    |
| `projects`                      | `projects_select`                                | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `public_intake_links`           | `intake_links_modify_internal`                   | ALL    | `using ( org_id in (select user_org_ids()) and ( is_org_admin(org_id) or department_id in (select user_department_ids()) ) ) with check ( org_id in (select user_org_ids()) and ( is_org_admin(org_id) or` |
| `public_intake_links`           | `intake_links_select_internal`                   | SELECT | `using ( org_id in (select user_org_ids()) and ( is_org_admin(org_id) or department_id in (select user_department_ids()) ) )`                                                                              |
| `public_intake_links`           | `intake_links_select_public_anon`                | SELECT | `to anon using ( is_active = true and (expires_at is null or expires_at > now()) )`                                                                                                                        |
| `recurring_task_assignments`    | `rta_modify`                                     | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `recurring_task_assignments`    | `rta_select`                                     | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `recurring_tasks`               | `recurring_tasks_modify`                         | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `recurring_tasks`               | `recurring_tasks_select`                         | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `report_runs`                   | `report_runs_insert`                             | INSERT | `WITH CHECK ( public.is_manager(org_id) OR public.is_instructor(org_id) )`                                                                                                                                 |
| `report_runs`                   | `report_runs_select`                             | SELECT | `using ( org_id in (select user_org_ids()) and ( is_org_admin(org_id) or department_id in (select user_department_ids()) ) )`                                                                              |
| `saved_reports`                 | `saved_reports_modify_own`                       | ALL    | `using ( created_by = auth.uid() and org_id in (select user_org_ids()) and ( is_org_admin(org_id) or department_id in (select user_department_ids()) ) ) with check ( created_by = auth.uid() and org_id ` |
| `saved_reports`                 | `saved_reports_select`                           | SELECT | `using ( org_id in (select user_org_ids()) and ( is_org_admin(org_id) or department_id in (select user_department_ids()) ) )`                                                                              |
| `skills`                        | `skills_modify`                                  | ALL    | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `skills`                        | `skills_select`                                  | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `support_ticket_messages`       | `ticket_messages_insert`                         | INSERT | `with check ( exists ( select 1 from public.support_tickets t where t.id = ticket_id and ( (author_kind = 'user' and t.user_id = auth.uid()) or (author_kind = 'admin' and public.is_org_admin(t.org_id))` |
| `support_ticket_messages`       | `ticket_messages_select`                         | SELECT | `using ( exists ( select 1 from public.support_tickets t where t.id = ticket_id and ( t.user_id = auth.uid() or (t.org_id in (select public.user_org_ids()) and public.is_org_admin(t.org_id)) ) ) )`      |
| `support_tickets`               | `support_tickets_insert_own`                     | INSERT | `with check ( user_id = auth.uid() and org_id in (select public.user_org_ids()) )`                                                                                                                         |
| `support_tickets`               | `support_tickets_select`                         | SELECT | `using ( user_id = auth.uid() or (org_id in (select public.user_org_ids()) and public.is_org_admin(org_id)) )`                                                                                             |
| `support_tickets`               | `support_tickets_update`                         | UPDATE | `using ( user_id = auth.uid() or (org_id in (select public.user_org_ids()) and public.is_org_admin(org_id)) ) with check ( user_id = auth.uid() or (org_id in (select public.user_org_ids()) and public.i` |
| `task_action_items`             | `task_action_items_instructor_assigned`          | ALL    | `USING ( public.is_instructor(task_action_items.org_id) AND EXISTS ( SELECT 1 FROM public.task_assignments ta JOIN public.project_team_members ptm ON ptm.id = ta.project_team_member_id WHERE ta.task_id` |
| `task_action_items`             | `task_action_items_manager_all`                  | ALL    | `USING (public.is_manager(org_id)) WITH CHECK (public.is_manager(org_id))`                                                                                                                                 |
| `task_action_items`             | `task_action_items_modify`                       | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `task_action_items`             | `task_action_items_select`                       | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `task_assignments`              | `task_assignments_modify`                        | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `task_assignments`              | `task_assignments_select`                        | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `task_dependencies`             | `task_dependencies_modify`                       | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `task_dependencies`             | `task_dependencies_select`                       | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `tasks`                         | `tasks_instructor_assigned_update`               | UPDATE | `USING ( public.is_instructor(tasks.org_id) AND EXISTS ( SELECT 1 FROM public.task_assignments ta JOIN public.project_team_members ptm ON ptm.id = ta.project_team_member_id WHERE ta.task_id = tasks.id ` |
| `tasks`                         | `tasks_manager_all`                              | ALL    | `USING (public.is_manager(org_id)) WITH CHECK (public.is_manager(org_id))`                                                                                                                                 |
| `tasks`                         | `tasks_modify`                                   | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `tasks`                         | `tasks_public_share_select`                      | SELECT | `to anon using ( exists ( select 1 from public.projects p where p.id = tasks.project_id and p.public_share_token is not null and p.public_share_token = public.get_pg_share_token() ) )`                   |
| `tasks`                         | `tasks_select`                                   | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `tra_deliverables`              | `tra_deliverables_modify`                        | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `tra_deliverables`              | `tra_deliverables_select`                        | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
| `tras`                          | `tras_instructor_insert`                         | INSERT | `WITH CHECK ( public.is_instructor(org_id) AND department_id IN (SELECT public.user_department_ids()) AND created_by = auth.uid() )`                                                                       |
| `tras`                          | `tras_instructor_update_own`                     | UPDATE | `USING ( public.is_instructor(org_id) AND created_by = auth.uid() AND status IN ('draft', 'documented') ) WITH CHECK ( public.is_instructor(org_id) AND created_by = auth.uid() AND status IN ('draft', '` |
| `tras`                          | `tras_manager_all`                               | ALL    | `USING (public.is_manager(org_id)) WITH CHECK (public.is_manager(org_id))`                                                                                                                                 |
| `tras`                          | `tras_modify`                                    | ALL    | `using (org_id in (select public.user_org_ids())) with check (org_id in (select public.user_org_ids()))`                                                                                                   |
| `tras`                          | `tras_select`                                    | SELECT | `using (org_id in (select public.user_org_ids()))`                                                                                                                                                         |
